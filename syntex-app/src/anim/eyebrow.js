// Eyebrow micro-animation is now handled by CSS + framer-motion (see .eyebrow class).
// This module is kept as a no-op so existing imports keep working.
export function initEyebrows(/* root=document */){
  return () => {}
}
